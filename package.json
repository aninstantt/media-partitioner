{
  "name": "media-partitioner",
  "version": "0.1.0",
  "private": true,
  "description": "Split audio or video files into tagged MP3 tracks with FFmpeg.",
  "bin": {
    "media-partitioner": "./partitioner.js"
  },
  "scripts": {
    "start": "node partitioner.js",
    "help": "node partitioner.js --help",
    "test": "node validate-output.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "type": "module"
}
