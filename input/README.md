Put your source media files here.

Examples:
- video files: `.mp4`, `.mkv`, `.mov`
- audio files: `.mp3`, `.wav`, `.m4a`
- cover images: `.jpg`, `.jpeg`, `.png`

The `segments.config.json` file should reference paths relative to this `input/` directory.
